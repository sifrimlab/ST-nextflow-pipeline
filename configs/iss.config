params {
        // User data:
        //reference = "${dataDir}DO/REF.${extension}" 
        //codebook = "${dataDir/codebook.csv}"
        // Normalization
        clip_percentile = 80
        // White tophat filter:
        filter_radius=15
        // Spot detection:
        min_sigma = 1
        max_sigma = 10
}