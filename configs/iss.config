params {
        // User data:
        reference = "/path/to/reference/image" 
        codebook = "/path/to/codebook.csv"
        round_prefix = "Round*"
        extension = "tif"

        ////////////
        // Pipeline parameters
        ///////////
        
        // Normalization
        clip_percentile = 80

        // White tophat filter:
        filter_radius=15

        // Spot detection:
        min_sigma = 1
        max_sigma = 10
}